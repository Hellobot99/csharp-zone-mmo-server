# 부하테스트 결과

`Server/src/TestClient` 봇 클라이언트로 로컬 PC → Azure Korea Central VM에 대고 진행한
기록. 원본 상세 데이터(초당 추이 등)는 [`Server/docs/performance.md`](Server/docs/performance.md)에
있고, 여기서는 핵심 수치와 최적화 과정만 정리한다.

## 테스트 환경
- 서버: Azure VM (Korea Central), docker compose로 gameserver 단독 구동
- 클라: 로컬 PC → Azure (실제 네트워크 왕복 포함)
- 측정: Move/Chat RTT (avg/p95/max), 인증 성공률, 매치 수

## 1단계 — 베이스라인에서 한계를 실제로 찾음

| 봇 수 | Move avg | Move p95 | Chat avg | 비고 |
|---|---|---|---|---|
| 50 | 163ms | 675ms | 56ms | 안정 |
| 100 | 336ms | 912ms | 204ms | 저하 시작 |
| 150 | 667ms | 4,125ms | 209ms | p95 4초대 — 병목 명확 |

**300봇에서는 실제로 무너졌다** — Move avg 2,009ms, ZoneTransfer 성공률 84.8%.
IOCP/Idle-Runtime 프로젝트가 "봇 수를 올려도 안 무너져서 진짜 한계를 못 찾았다"고
기록한 것과 달리, 이 프로젝트는 **한계 지점을 실제로 관측했다.**

### 원인
1. **브로드캐스트 증폭(O(N²))** — Zone 안의 모든 세션에게 Move 패킷을 전체 전송.
   N명이 있을 때 한 명이 움직이면 N개의 패킷이 나가므로 총 전송량이 인원수의
   제곱에 비례해 증가한다.
2. **`Task.Run` 오버헤드** — 패킷 수신마다 스레드풀에 작업을 등록하는 방식이라,
   패킷이 몰릴수록 큐 대기 + 컨텍스트 스위치 비용이 같이 늘어난다.

## 2단계 — Channel 전환
송신 큐를 `ConcurrentQueue + 플래그` 방식에서 `System.Threading.Channels` 기반
전용 write 루프로 교체 (경합 제거, 순서 보장).

| 지표 (150봇) | 이전 | 이후 | 개선 |
|---|---|---|---|
| Move avg | 667ms | 298ms | **-55%** |
| Move p95 | 4,125ms | 813ms | **-80%** |
| Chat avg | 209ms | 80ms | **-62%** |

## 3단계 — AOI(관심 영역) 도입, 그리고 예상 밖의 역효과
Move 브로드캐스트를 "존 전체 전송"에서 "좌표 기준 반경 이내만 전송"으로 바꿨다
(range=300). 150봇 이하에서는 확실히 개선됐지만:

| 봇 수 | Move avg (Channel) | Move avg (AOI r=300) | 결과 |
|---|---|---|---|
| 150 | 298ms | **190ms** | ✅ -36% |
| 200 | 275ms | **704ms** | ❌ +156% 악화 |
| 300 | 322ms | **641ms** | ❌ +99% 악화 |

**정직하게 실패를 기록**: 로비가 840×840 크기인데 range=300이면 로비에 150명 이상
몰렸을 때 그 반경 안에 이미 수십 명이 들어와서 "걸러내는 효과"가 거의 없어지고,
오히려 거리 계산 오버헤드만 추가된다. 필터링이 실질적으로 작동 안 하는 상황.

## 4단계 — AOI range를 150으로 축소해서 재조정
로비 대비 수신 면적 비율을 ~40% → ~10%로 좁혔다.

| 봇 수 | Move avg (r=300) | Move avg (r=150) | 개선 |
|---|---|---|---|
| 200 | 704ms | **351ms** | **-50%** |
| 300 | 641ms | 575ms | -10% |

| 봇 수 | Chat avg (r=300) | Chat avg (r=150) | 개선 |
|---|---|---|---|
| 300 | 368ms | **150ms** | **-59%** |

동시에 TCP 인증 재시도 로직을 "연결 재사용"에서 "연결 재생성"으로 바꿔서
`auth_fail`도 0으로 만들었다.

## 누적 개선 요약 (150봇 기준, 베이스라인 대비)

| 지표 | 베이스라인 | Channel | AOI(r=150) | 누적 개선 |
|---|---|---|---|---|
| Move avg | 667ms | 298ms | 190ms* | **-72%** |
| Move p95 | 4,125ms | 813ms | 694ms* | **-83%** |
| Chat avg | 209ms | 80ms | 62ms* | **-70%** |

\* AOI range=300 시점 수치 (150봇에서는 r=300이 r=150보다 근소하게 나음 — range는
동시접속자 수에 따라 다시 튜닝해야 하는 값이라는 것 자체가 이 실험의 결론)

## 배운 것
- 최적화가 항상 개선으로 이어지지 않는다 — AOI range=300은 저인원에선 좋고
  고인원에선 나빴다. **"효과가 있다/없다"가 아니라 "어떤 조건에서 효과가 있는지"**
  까지 재보고 나서야 결론을 낼 수 있었다.
- 채팅(Chat)은 의도적으로 AOI를 안 걸고 전체 브로드캐스트를 유지했다 — 글로벌
  채팅 특성상 "근처 사람한테만 보이는 채팅"이 오히려 이상하기 때문. 모든 걸
  AOI로 거를 필요는 없다.
